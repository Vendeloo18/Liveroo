{
  "name": "@vendeloo/rules-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-vendeloo \"node --test firestore.test.mjs\"",
    "test:welcome-bonus": "pnpm --filter functions build && firebase emulators:exec --only auth,firestore,functions --project demo-vendeloo \"node --test welcome-bonus.test.mjs\"",
    "test:engine": "firebase emulators:exec --only firestore,functions --project demo-vendeloo \"node --test engine.test.mjs\"",
    "test:scale:smoke": "VENDELOO_SCALE_AUCTIONS=10 VENDELOO_SCALE_BIDS_PER_AUCTION=10 firebase emulators:exec --only firestore,functions --project demo-vendeloo \"node scale.test.mjs\"",
    "test:scale": "firebase emulators:exec --only firestore,functions --project demo-vendeloo \"node scale.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.11.0",
    "firebase-admin": "^12.0.0"
  }
}
